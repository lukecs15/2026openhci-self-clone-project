# Voice Clone 前端環境變數範本，複製為 .env 使用

# 後端 WebSocket 位址（voice_clone_backend 預設監聽 8200）
VITE_WS_BASE_URL=ws://localhost:8200

# 後端 REST API 位址（聲音克隆 profile 上傳/建立用）
VITE_API_BASE_URL=http://localhost:8200/api

# 手機 onboarding 前端的網址（QR code 內容會指向這裡帶 session id），
# voice_clone_mobile 專案預設跑在 5175（尚未建立時 QR 掃了也連不到，
# 開場畫面有「略過連結」按鈕可以先用預設 demo agent 測試其餘功能）。
VITE_MOBILE_FRONTEND_URL=http://localhost:5175
