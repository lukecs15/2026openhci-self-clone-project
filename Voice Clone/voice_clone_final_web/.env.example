# ── 後端位址 ─────────────────────────────────────────────────────────
# 兩個都「留空」＝走同源相對路徑（/api 與同源 wss），由 vite dev server
# 的 proxy 轉發到 localhost:8200——本機開發與 cloudflared tunnel 部署都
# 直接可用，不需要改任何值（見 vite.config.js「cloudflared 部署」說明）。
# 只有要讓前端「直連」另一台後端時才填絕對網址。
VITE_API_BASE_URL=
VITE_WS_BASE_URL=

# ── 手機端（voice_clone_mobile）對外網址 ─────────────────────────────
# QR code 會編碼：
#   <MOBILE_BASE_URL>/link?session=<id>（上傳問卷+聲音）
#   <MOBILE_BASE_URL>/result?session=<id>（領取報告）
# 手機是「另一台裝置」，一定要填手機連得到的網址：
#   cloudflared 部署 → 填 mobile 那條 tunnel 的網址（https://xxxx.trycloudflare.com）
#   區網測試       → 填 http://<這台電腦的區網IP>:5173
VITE_MOBILE_BASE_URL=http://localhost:5173

# ── 體驗參數 ─────────────────────────────────────────────────────────
# 每個情境的辯論回合上限（達到後請使用者做出選擇）
VITE_DEBATE_MAX_TURNS=8
# 每個情境的使用者介入次數上限
VITE_MAX_INTERVENTIONS=3

# ── cloudflared 部署備忘 ─────────────────────────────────────────────
# 1. 後端：uvicorn main:app --host 0.0.0.0 --port 8200（不需要 tunnel）
# 2. final web：npm run dev（port 5174）
#    cloudflared tunnel --url http://localhost:5174   ← 展場機/平板開這個網址
# 3. mobile：npm run dev（port 5173）
#    cloudflared tunnel --url http://localhost:5173   ← 網址填進上面的
#    VITE_MOBILE_BASE_URL（改 .env 後要重啟 final web 的 dev server）
# 手機錄音（getUserMedia）需要 https，tunnel 剛好滿足；final web 的
# 介入錄音同理，展場機用 tunnel 網址或 localhost 開啟即可。
