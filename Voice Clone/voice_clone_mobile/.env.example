# voice_clone_mobile 環境變數範本，複製為 .env 使用

# 後端 REST API 位址（跟 voice_clone_frontend 打的是同一個後端）。
# 用手機實機測試時，localhost 對手機來說是手機自己，要換成電腦的區網 IP，
# 例如 http://192.168.1.23:8200/api，且後端 main.py 的 CORS
# mobile_frontend_origin 也要設成這個網址對應的手機前端 origin。
VITE_API_BASE_URL=http://localhost:8200/api
